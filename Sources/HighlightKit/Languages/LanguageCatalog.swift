import Foundation

/// Namespace listing every bundled language grammar.
///
/// Each language lives in its own file as a `LanguageDescriptor`
/// extension; `all` is the registration list used by
/// ``Highlighter/init(languages:)`` by default.
public enum LanguageCatalog {
    /// Every bundled language.
    public static let all: [LanguageDescriptor] = [
        ada,
        apache,
        applescript,
        armasm,
        bash,
        basic,
        c,
        clojure,
        cmake,
        coffeescript,
        cpp,
        csharp,
        css,
        dart,
        delphi,
        diff,
        dockerfile,
        dos,
        elm,
        elixir,
        erlang,
        fortran,
        fsharp,
        go,
        graphql,
        groovy,
        haskell,
        http,
        ini,
        java,
        javascript,
        json,
        kotlin,
        leaf,
        less,
        lisp,
        llvm,
        lua,
        makefile,
        markdown,
        matlab,
        nginx,
        nim,
        nix,
        objectivec,
        ocaml,
        perl,
        php,
        plaintext,
        powershell,
        prolog,
        properties,
        protobuf,
        python,
        r,
        ruby,
        rust,
        scala,
        scheme,
        scss,
        shell,
        sql,
        stylus,
        swift,
        terraform,
        typescript,
        vim,
        xml,
        yaml,
    ]
}
